spring:
  liquibase:
    enabled: false
  application:
    name: transaction-cursor
  cloud:
    kubernetes:
      config:
        enabled: false
      enabled: false
      discovery:
        enabled: true
