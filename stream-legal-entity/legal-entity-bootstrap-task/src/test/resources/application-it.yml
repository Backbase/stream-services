spring:
  zipkin:
    enabled: true

eureka:
  client:
    enabled: false

backbase:
  stream:
    client:
      additional-headers:
        X-TID: tenant1

logging:
  level:
    com.backbase.stream: DEBUG
    org.springframework.cloud: DEBUG
    org.springframework.boot.availability: DEBUG
    brave: DEBUG
    zipkin2: DEBUG
