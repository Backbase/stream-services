spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

backbase:
  stream:
    client:
      additional-headers:
        X-TID: tenant1

logging:
  level:
    com.backbase.stream: DEBUG
