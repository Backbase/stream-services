---
date: 2026-05-12T18:08:00+03:00
reviewer: Roman Kniazevych
git_commit: a6612f7e343b6944924980770654913f308637c2
branch: TAR-738
repository: stream-services
jira_ticket: TAR-738
preceding_artifacts:
  [docs/specs/TAR-738/jira-task.md, docs/specs/TAR-738/solution-design-2026-05-12-investment-service-api-upgrade.md, docs/specs/TAR-738/execution-plan-2026-05-12-investment-service-api-upgrade.md]
topic: investment-service-api-upgrade
review_type: architecture-review
solution_design: docs/specs/TAR-738/solution-design-2026-05-12-investment-service-api-upgrade.md
status: draft
last_updated: 2026-05-12
last_updated_by: Roman Kniazevych
---

# Architecture Review: TAR-738

**Date:** 2026-05-12
**Agent:** Architect Agent
**Mode:** JUDGE
**Verdict:** ✅ APPROVED

---

## Review Scope

**Solution Design:** `docs/specs/TAR-738/solution-design-2026-05-12-investment-service-api-upgrade.md`

**Selected Technical Standards:** Derived from solution-design Section 1 "Technical Standards Compliance" (no formal TS-XXX IDs; sourced from `AGENTS.md` conventions):
- Never hand-edit `target/generated-sources/` (AGENTS.md)
- Lombok + MapStruct conventions (AGENTS.md)
- Reactive/non-blocking pattern (AGENTS.md)

**Files Reviewed:**
- `stream-investment/investment-core/pom.xml`
- `stream-investment/investment-core/src/main/java/com/backbase/stream/investment/service/InvestmentPortfolioService.java`
- `stream-investment/investment-core/src/test/java/com/backbase/stream/investment/service/InvestmentPortfolioAllocationServiceTest.java`
- `stream-investment/investment-core/src/test/java/com/backbase/stream/investment/service/InvestmentPortfolioServiceTest.java`
- `CHANGELOG.md`

---

## Technical Standards Compliance

| Standard | How Satisfied (from solution-design §1) | Status | Evidence |
|---|---|---|---|
| Never hand-edit `target/generated-sources/` | All changes confined to `pom.xml`, production source, and test source; generated sources regenerated by Maven | ✅ | `pom.xml:18` bumped; `InvestmentPortfolioService.java` and test files edited; no files under `target/` committed |
| Lombok + MapStruct conventions | No new manual code introduced; existing `@Slf4j`, `@RequiredArgsConstructor` patterns preserved | ✅ | `InvestmentPortfolioService.java:66–67` retains `@Slf4j`/`@RequiredArgsConstructor`; no manual mapping code added |
| Reactive/non-blocking pattern | No reactive pipeline changes; version bump only affects generated client method signatures | ✅ | `InvestmentPortfolioService.java:403–407` preserves existing `Mono` chain; constructor changes are structural only |

---

## Structure Compliance

| Check | Status | Evidence |
|---|---|---|
| Matches solution-design §2.1 Design Structure | ✅ | `pom.xml:18` `investment-service-api.version` = `1.6.0`; `boat-maven-plugin` executions unchanged (lines 96–135); both `v1` and `sync.v1` packages regenerated |
| Matches solution-design §2.2 Data Flow | ✅ | Flow steps 1–8 all confirmed: version bump → `mvn generate-sources` → codegen → `mvn compile` errors identified → production fixed → `mvn test-compile` errors identified → test fixtures fixed → `mvn clean install` exits 0 |
| Matches solution-design §2.3 State | ✅ | API version stored in `pom.xml` property (`investment-service-api.version:18`); generated sources remain transient in `target/` (not committed to VCS) |
| No layer violations (service imports only generated client interfaces) | ✅ | `InvestmentPortfolioService.java` imports unchanged; no new cross-layer dependencies introduced |
| Class complexity ≤10 public methods | ✅ | `InvestmentPortfolioService` has 6 public methods: `upsertPortfolios`, `upsertInvestmentProducts`, `upsertInvestmentPortfolios`, `upsertPortfolioTradingAccounts`, `upsertPortfolioTradingAccount`, `createPortfolioTradingAccount` |

---

## Changes Verified

| File | Expected Change (solution-design §4) | Status |
|---|---|---|
| `stream-investment/investment-core/pom.xml` | Update `investment-service-api.version` from `1.4.1` to `1.6.0` | ✅ Line 18 |
| `InvestmentPortfolioService.java` | Fix usages of changed model classes/API client methods | ✅ Lines 150–154 (constructors) and 403–407 (`listPortfolioProducts` 13-arg signature) |
| `InvestmentSaga.java` | Fix if needed | ✅ Not needed — compiler reported no errors |
| `InvestmentAssetUniverseSaga.java` | Fix if needed | ✅ Not needed |
| `InvestmentContentSaga.java` | Fix if needed | ✅ Not needed |
| `InvestmentAssetUniverseService.java` | Fix if needed | ✅ Not needed |
| `InvestmentRiskAssessmentService.java` | Fix if needed | ✅ Not needed |
| `InvestmentClientService.java` | Fix if needed | ✅ Not needed |
| `InvestmentRestAssetUniverseService.java` | Fix if needed (sync models) | ✅ Not needed |
| `InvestmentClientConfig.java` | Fix if needed (WebClient interface signatures) | ✅ Not needed |
| `InvestmentRestServiceApiConfiguration.java` | Fix if needed (RestTemplate bean types) | ✅ Not needed |
| `src/test/java/**/*Test.java` (affected files) | Update fixture construction to match 1.6.0 model field structure | ✅ `InvestmentPortfolioAllocationServiceTest.java:634` and `InvestmentPortfolioServiceTest.java` (5 stubs at lines 949–1107) |
| `CHANGELOG.md` | Add entry under next version heading | ✅ Entry added under `[10.2.0]` |

---

## Edge Cases

| Scenario | Status | Evidence |
|---|---|---|
| Loading | ✅ N/A | solution-design §5: "No runtime loading state in a build upgrade task" — confirmed; no runtime behaviour changed |
| Empty | ✅ N/A | solution-design §5: "1.6.0 API introduces no breaking changes affecting investment-core" — confirmed by BUILD SUCCESS |
| Error — test fixtures reference old model fields | ✅ | solution-design §5 Detailed: `mvn test-compile` failures identified; 7-arg `InvestorModelPortfolio` constructor and 13-arg `listPortfolioProducts` stubs corrected in 2 test files |
| Error — production code references removed/renamed API methods | ✅ | solution-design §5 Detailed: `mvn compile` failures identified; constructor and method signature corrected in `InvestmentPortfolioService.java` |
| Error — new enum values require additional `enumNameMappings` | ✅ N/A | solution-design §5 Detailed: Not triggered; existing `Etc/GMT-12=ETC_GMT_1222` mapping sufficient for 1.6.0; `mvn generate-sources` exits 0 |

---

## Blockers (if any)

None.

---

## Warnings (if any)

None for this implementation. The pre-existing double-retry pattern (`InvestmentPortfolioService.java:508–509`) noted in the code review is outside scope of this ticket.

---

## Verdict

**APPROVED — 0 violations.** Implementation fully complies with the approved solution design. All three technical standards are satisfied, the design structure and data flow match the solution-design exactly, all expected file changes are implemented (with "fix if needed" files correctly unchanged where the compiler reported no errors), and all three documented edge cases are handled per solution-design §5.
